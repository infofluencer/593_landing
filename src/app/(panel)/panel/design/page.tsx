import DesignSystemPreview from "@/components/panel/ds/DesignSystemPreview";

/** Staff-only foundation preview — sayfa giydirme onayı için. */
export default function DesignSystemPage() {
  return <DesignSystemPreview />;
}
