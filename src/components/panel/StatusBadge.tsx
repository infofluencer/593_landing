/**
 * @deprecated Prefer `@/components/panel/ds/StatusBadge`.
 * Re-export keeps existing page imports working during the design migration.
 */
export { StatusBadge } from "@/components/panel/ds/StatusBadge";
